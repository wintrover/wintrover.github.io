---
title: "KYC 비디오 녹화 & 분할 시스템 만들기: React + FFmpeg + MediaRecorder API"
date: 2025-08-31 00:00:00 -0000
tags: SMBholdings
category: Company Work
excerpt: "KYC 절차에서 사용자의 얼굴 인증 전체 과정을 녹화하고, React와 FFmpeg로 단계별 세그먼트로 분할해 저장하는 시스템을 개발한 경험을 공유합니다."

description: "React, FFmpeg.wasm, MediaRecorder API를 활용해 KYC 비디오 녹화 및 분할 시스템을 개발하면서, 웹 기반 비디오 처리에서의 Cross-Origin 정책/헤더 문제를 해결한 과정을 정리했습니다."
---


## Project Overview

KYC(Know Your Customer) 절차에서 사용자의 얼굴 인증 전체 과정을 **하나의 비디오로 녹화**하고, 이를 **단계별(미션별) 세그먼트로 분할**해 저장하는 시스템을 개발했습니다. 이 프로젝트는 **React**, **TypeScript**, **FFmpeg**, **MediaRecorder API**를 핵심 기술로 사용했고, 웹 환경에서 실시간 비디오 처리를 구현해야 하는 까다로운 문제를 풀어야 했습니다.

## 🎯 핵심 기능 구현

### 시스템 아키텍처 개요

### 1. 비디오 녹화 시스템

KYC 미션 시작부터 완료까지의 전체 과정을 **MediaRecorder API**로 녹화하는 시스템을 구현했습니다.

```typescript
// Start video recording
const startRecording = async () => {
  const stream = await navigator.mediaDevices.getUserMedia({ video: true, audio: true });
  const mediaRecorder = new MediaRecorder(stream);

  mediaRecorder.ondataavailable = (event) => {
    if (event.data.size > 0) {
      recordedChunks.push(event.data);
    }
  };

  mediaRecorder.start();
};
```

각 KYC 단계별 시작/종료 타임스탬프를 정확히 기록하기 위해 **타임라인 데이터 구조**를 설계했습니다.

```typescript
interface KYCTimeline {
  stepIndex: number;
  stepName: string;
  startTime: number;
  endTime: number;
  duration: number;
}
```

### 2. 웹 환경에서 FFmpeg 세팅

브라우저에서 FFmpeg를 사용하려면 여러 기술적 난관이 있습니다. 특히 WASM 기반 FFmpeg는 SharedArrayBuffer를 필요로 하는 경우가 많아, 보안 헤더 설정이 필수였습니다.

#### Cross-Origin 헤더 설정

SharedArrayBuffer 사용을 위한 필수 헤더 설정:

```typescript
// vite.config.ts
export default defineConfig({
  server: {
    headers: {
      'Cross-Origin-Opener-Policy': 'same-origin',
      'Cross-Origin-Embedder-Policy': 'require-corp'
    }
  }
});
```

#### FFmpeg 초기화

```typescript
import { FFmpeg } from '@ffmpeg/ffmpeg';
import { toBlobURL } from '@ffmpeg/util';

const ffmpeg = new FFmpeg();

const loadFFmpeg = async () => {
  const baseURL = 'https://unpkg.com/@ffmpeg/core@0.12.10/dist/esm';

  await ffmpeg.load({
    coreURL: await toBlobURL(`${baseURL}/ffmpeg-core.js`, 'text/javascript'),
    wasmURL: await toBlobURL(`${baseURL}/ffmpeg-core.wasm`, 'application/wasm')
  });
};
```

### 3. 비디오 분할 & 처리 파이프라인

타임라인 데이터를 기반으로 비디오를 단계별로 분할하는 시스템을 구현했습니다.

#### 비디오 처리 흐름

```typescript
const splitVideo = async (videoBlob: Blob, timeline: KYCTimeline[]) => {
  // Write video to FFmpeg virtual file system
  await ffmpeg.writeFile('input.webm', await fetchFile(videoBlob));

  const splitVideos = [];

  for (const [index, step] of timeline.entries()) {
    const startTime = formatTime(step.startTime);
    const duration = formatTime(step.duration);
    const outputName = `step${index}_${step.stepName}.webm`;

    // Split video with FFmpeg command
    await ffmpeg.exec([
      '-i', 'input.webm',
      '-ss', startTime,
      '-t', duration,
      '-c', 'copy',
      outputName
    ]);

    // Read split video
    const data = await ffmpeg.readFile(outputName);
    const blob = new Blob([data], { type: 'video/webm' });
    splitVideos.push({ name: outputName, blob });
  }

  return splitVideos;
};
```

#### 시간 포맷 변환 유틸리티

```typescript
const formatTime = (milliseconds: number): string => {
  const totalSeconds = Math.floor(milliseconds / 1000);
  const hours = Math.floor(totalSeconds / 3600);
  const minutes = Math.floor((totalSeconds % 3600) / 60);
  const seconds = totalSeconds % 60;
  const ms = milliseconds % 1000;

  return `${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}.${ms.toString().padStart(3, '0')}`;
};
```

## 🔧 주요 시행착오

### Cross-Origin 정책과 보안 헤더의 관계

### 1. OpenCV.js COEP Cross-Origin 이슈

**문제**: OpenCV.js를 CDN에서 로딩할 때 `net::ERR_BLOCKED_BY_RESPONSE.NotSameOriginAfterDefaultedToSameOriginByCoep` 에러가 발생하며 로딩 실패

**해결**: CDN 대신 로컬 호스팅 방식으로 전환

```typescript
// Previous CDN approach (failed)
// <script src="https://docs.opencv.org/4.x/opencv.js"></script>

// Local hosting approach (success)
// After downloading public/opencv.js file
<script src="/opencv.js"></script>
```

### 2. 비디오 중복 업로드 방지

**문제**: 동일한 비디오가 여러 번 업로드되는 현상 발생

**해결**: 상태 관리와 참조 비교로 중복 업로드 차단

```typescript
const [isVideoUploaded, setIsVideoUploaded] = useState(false);
const uploadedBlobRef = useRef<Blob | null>(null);

const uploadVideo = async (videoBlob: Blob) => {
  // Prevent duplicate uploads
  if (isVideoUploaded && uploadedBlobRef.current === videoBlob) {
    return;
  }

  try {
    const formData = new FormData();
    formData.append('video', videoBlob, 'kyc-recording.webm');

    await fetch('/api/upload-video', {
      method: 'POST',
      body: formData
    });

    setIsVideoUploaded(true);
    uploadedBlobRef.current = videoBlob;
  } catch (error) {
    console.error('Video upload failed:', error);
  }
};
```

### 3. 메모리 관리와 성능 최적화

**문제**: 큰 비디오를 처리할 때 메모리 부족 및 성능 저하 발생

**해결**: 리소스 관리 및 정리(cleanup) 로직을 확실히 적용

```typescript
const processVideo = async (videoBlob: Blob) => {
  try {
    // Video processing logic
    const splitVideos = await splitVideo(videoBlob, timeline);

    // Upload split videos
    const uploadResults = await Promise.allSettled(
      splitVideos.map(video => uploadSplitVideo(video))
    );

    // Count success/failure
    const successful = uploadResults.filter(result => result.status === 'fulfilled').length;
    const failed = uploadResults.filter(result => result.status === 'rejected').length;

    if (failed > 0) {
      console.error(`Video upload failed: ${failed}, successful: ${successful}`);
    }
  } finally {
    // Memory cleanup
    await cleanupFFmpegFiles();
  }
};

const cleanupFFmpegFiles = async () => {
  try {
    const files = ['input.webm', ...timeline.map((_, i) => `step${i}_output.webm`)];
    for (const file of files) {
      try {
        await ffmpeg.deleteFile(file);
      } catch {
        // Ignore if file doesn't exist
      }
    }
  } catch (error) {
    console.error('FFmpeg file cleanup failed:', error);
  }
};
```

## 🚀 기술 스택 및 아키텍처

### 기술 스택 다이어그램

### 프론트엔드 기술 스택
- **React 19** + **TypeScript**: 컴포넌트 기반 UI 개발
- **@tanstack/react-query**: 서버 상태 관리
- **@tanstack/react-router**: 라우팅
- **Styled Components**: CSS-in-JS 스타일링
- **TailwindCSS**: 유틸리티 기반 스타일링

### 비디오 처리 기술
- **MediaRecorder API**: 실시간 비디오 녹화
- **FFmpeg.wasm**: 웹 환경에서의 비디오 처리
- **OpenCV.js**: 컴퓨터 비전 처리
- **MediaPipe**: 얼굴 인식 및 트래킹

### 개발 도구
- **Vite**: 빌드 도구 및 개발 서버
- **Jest**: 유닛 테스트
- **SonarQube**: 코드 품질 분석
- **Sentry**: 에러 모니터링

## 📈 성과와 배운 점

### 개발 과정 및 성능 지표

### 성과
1. **웹 환경에서 실시간 비디오 처리 시스템**을 완성
2. **FFmpeg.wasm 기반 클라이언트 사이드 비디오 분할** 구현
3. **Cross-Origin 정책과 SharedArrayBuffer** 관련 이슈 해결
4. **메모리 효율적인 대용량 파일 처리** 로직 구현

### 핵심 배움
1. **웹 보안 정책**: COEP/COOP 헤더와 외부 리소스 로딩 제약의 중요성
2. **WebAssembly 활용**: 네이티브 급 성능이 필요한 작업을 웹에서 구현하는 접근
3. **비동기 처리 최적화**: Promise.allSettled로 안정적인 병렬 처리 구성
4. **리소스 관리**: 메모리 누수 방지와 올바른 정리(클린업)의 중요성

## 🔮 향후 개선 방향

1. **Web Workers 활용**: 메인 스레드 블로킹 방지를 위한 백그라운드 처리
2. **Progressive Upload**: 대용량 파일을 위한 청크 단위 업로드
3. **실시간 압축**: 녹화 중 실시간 압축으로 파일 크기 최적화
4. **오프라인 지원**: Service Worker 기반 네트워크 불안정 대응

---

이 프로젝트를 통해 웹 환경에서의 고급 비디오 처리 기법을 익힐 수 있었고, 브라우저 보안 정책과 WebAssembly 활용에 대해 깊이 이해하게 되었습니다. 앞으로도 웹 기술의 한계를 밀어붙이는 프로젝트에 계속 도전하고 싶습니다.
